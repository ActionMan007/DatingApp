export interface User{              //Interface in ts are different from interfaces in C#

    username: string;
    token: string;
}

//Using interface in Ts--Can be used to describe that something is a type of something